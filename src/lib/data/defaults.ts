// Module-tiny default benchmark name, split out so consumers (the leaderboard
// store; service loaders) don't pull in the multi-hundred-line mock fixtures
// in `mockBenchmarks.ts` just to read a single string.
export const DEFAULT_BENCHMARK_NAME = 'MTEB(Multilingual, v2)';
